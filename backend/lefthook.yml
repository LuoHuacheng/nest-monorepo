pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.ts"
      run: npx oxlint --fix {staged_files}
      stage_fixed: true

    - name: format
      glob: "*.ts"
      run: npx oxfmt {staged_files}
      stage_fixed: true
